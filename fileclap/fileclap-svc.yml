apiVersion: v1
kind: Service
metadata:
  name: fileclap-service
  namespace: default
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30083
  selector:
    app: fileclap-app
